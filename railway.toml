# Railway configuration for Fix2Runbook
# This file configures the monorepo deployment on Railway

[build]
builder = "DOCKERFILE"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
